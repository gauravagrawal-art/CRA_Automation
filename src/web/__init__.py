"""Local web UI for the CRA assessment workflow.

The UI orchestrates and explains; it never decides. Every verdict, count and
recommendation it shows was computed by the Python service layer and read back
from an artifact on disk.
"""
